/**
 * importer.styl
 *
 * By default, new Sails projects are configured to compile this file
 * from STYLUS to CSS.  Unlike CSS files, STYLUS files are not compiled and
 * included automatically unless they are imported below.
 *
 * The STYLUS files imported below are compiled and included in the order
 * they are listed.  Mixins, variables, etc. should be imported first
 * so that they can be accessed by subsequent STYLUS stylesheets.
 *
 * (Just like the rest of the asset pipeline bundled in Sails, you can
 * always omit, customize, or replace this behavior with SASS, SCSS, STYL
 * or any other Grunt tasks you like.)
 *
 * @file importer.styl
 * @author Pavel Anisimov <pavel.n.anisimov@gmail.com>
 * @copyright 2011-2015 Pavel Anisimov
 */

@import 'vars.styl'
@import 'bootstrap-custom.styl'
@import 'bootstrap-wrapper.styl'
@import 'main.styl'


